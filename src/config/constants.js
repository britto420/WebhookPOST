const PAYMENT_STATUS = {
  APPROVED: 'approved',
  PENDING: 'pending',
  REJECTED: 'rejected',
  CANCELLED: 'cancelled',
  EXPIRED: 'expired'
};

const NOTIFICATION_TYPES = {
  PAYMENT: 'payment'
};

module.exports = {
  PAYMENT_STATUS,
  NOTIFICATION_TYPES
};